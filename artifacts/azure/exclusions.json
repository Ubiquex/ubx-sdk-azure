{
  "skip_descriptions": {
    "azure_network_virtualnetwork_private_link_service": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_network_profile": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_network_security_group": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_private_endpoint": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_private_endpoint_connection": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_virtual_network": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_subnet": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_route_table": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_public_ipaddress": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_dscp_configuration": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_network_interface": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    },
    "azure_network_virtualnetwork_network_interface_tap_configuration": {
      "reason": "ARM's real, official virtualNetwork.json spec (Microsoft.Network/Network/stable/2025-09-01/virtualNetwork.json) is deeply cross-linked -- subnets embed NSGs, NSGs embed subnet references, private endpoints embed NIC/subnet objects, and so on -- so this resource type is one of 12 (of the entry's 37 real resource types) that recursively expand to 622,320 of the azure_network_virtualnetwork config entry's 622,782 total fields (99.9%) at a near-zero real sourced rate. Confirmed live via a full --dump-ir run plus a recursive per-resource field count, not estimated. Deeply-nested, cross-referenced network configuration nobody hand-authors, the same real pathology as AWS's own QuickSight Dashboard/Analysis/Template exclusion. describe_exclude applied in sdk/providers/.ubx/config (UBI-175 config-expansion phase); the other 25 resource types in the same config entry (462 fields total) are normal-sized and still get real description generation."
    }
  },
  "skip_page": {
    "azure_network_virtualnetwork_private_link_service": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_network_profile": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_network_security_group": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_private_endpoint": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_private_endpoint_connection": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_virtual_network": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_subnet": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_route_table": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_public_ipaddress": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_dscp_configuration": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_network_interface": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    },
    "azure_network_virtualnetwork_network_interface_tap_configuration": {
      "reason": "Page unpublished (not just description-skipped): this resource type is one of the 12 azure_network_virtualnetwork family members that recursively expand to 622,320 pathologically cross-linked fields (see this file's own skip_descriptions entry for the full reason). Unlike AWS QuickSight, whose exclusion was applied before any page was ever generated, this page WAS live with real content -- deep fields rendering bare mechanical placeholders (\"Optional.\"/\"Required.\") with zero real description, a genuine thin-content risk on a published, indexable page. Removed along with its nav entry; redirects to /resource-reference/azure for anyone hitting the old URL."
    }
  }
}
