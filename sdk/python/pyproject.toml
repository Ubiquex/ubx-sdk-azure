[project]
name = "ubx-sdk-azure"
version = "1.2.2"
description = "Generated Python bindings for azure@dynamic (ubx sdk gen). DO NOT EDIT."
dependencies = ["ubx_sdk>=0.2.0,<0.3.0"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["ubx*"]
namespaces = true
